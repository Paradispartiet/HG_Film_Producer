name: TEMP Chapter 19 CODA source-first

on:
  push:
    branches:
      - agent/ch19-coda-production-case
    paths:
      - .github/workflows/temp-ch19-coda-source-first.yml
      - src/core/chapterNineteenCodaExpansion.ts
      - src/core/chapterNineteenCodaExpansion.test.ts
      - src/ui/data/scenarioFilmStudyChapterNineteenCoda.ts
      - src/ui/data/scenarioProductionVerificationCoda.ts

permissions:
  contents: read

jobs:
  source-first:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Build core
        run: npm run build

      - name: Strict CODA contract
        run: node --test dist/core/chapterNineteenCodaExpansion.test.js

      - name: Build UI
        run: npm run build:ui
