name: TEMP Chapter 19 Poor Things source-first

on:
  push:
    branches:
      - agent/ch19-poor-things-production-case
    paths:
      - .github/workflows/temp-ch19-poor-things-source-first.yml
      - src/core/chapterNineteenPoorThingsExpansion.ts
      - src/core/chapterNineteenPoorThingsExpansion.test.ts
      - src/ui/data/scenarioFilmStudyChapterNineteenPoorThings.ts
      - src/ui/data/scenarioProductionVerificationPoorThings.ts

permissions:
  contents: read

jobs:
  source-first:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Build core
        run: npm run build

      - name: Strict Poor Things contract
        run: node --test dist/core/chapterNineteenPoorThingsExpansion.test.js

      - name: Build UI
        run: npm run build:ui
