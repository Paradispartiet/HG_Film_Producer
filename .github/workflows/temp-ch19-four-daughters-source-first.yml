name: TEMP Chapter 19 Four Daughters source-first

on:
  push:
    branches:
      - agent/ch19-four-daughters-production-case
    paths:
      - .github/workflows/temp-ch19-four-daughters-source-first.yml
      - src/core/chapterNineteenFourDaughtersExpansion.ts
      - src/core/chapterNineteenFourDaughtersExpansion.test.ts
      - src/ui/data/scenarioFilmStudyChapterNineteenFourDaughters.ts
      - src/ui/data/scenarioProductionVerificationFourDaughters.ts

permissions:
  contents: read

jobs:
  source-first:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Build core
        run: npm run build

      - name: Strict Four Daughters contract
        run: node --test dist/core/chapterNineteenFourDaughtersExpansion.test.js

      - name: Build UI
        run: npm run build:ui
