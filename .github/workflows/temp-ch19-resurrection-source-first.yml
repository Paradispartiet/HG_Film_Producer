name: TEMP Chapter 19 Resurrection source-first

on:
  push:
    branches:
      - agent/ch19-resurrection-production-case
    paths:
      - .github/workflows/temp-ch19-resurrection-source-first.yml
      - src/core/chapterNineteenResurrectionExpansion.ts
      - src/core/chapterNineteenResurrectionExpansion.test.ts
      - src/ui/data/scenarioProductionVerificationResurrection.ts
      - src/ui/data/scenarioFilmStudyChapterNineteenResurrection.ts

jobs:
  source-first:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout exact branch head
        uses: actions/checkout@v4
        with:
          ref: agent/ch19-resurrection-production-case
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Build core
        run: npm run build

      - name: Strict Resurrection contract
        run: node --test dist/core/chapterNineteenResurrectionExpansion.test.js

      - name: Build UI
        run: npm run build:ui
