name: TEMP Chapter 19 Saint Omer source-first

on:
  push:
    branches:
      - agent/ch19-saint-omer-production-case
    paths:
      - .github/workflows/temp-ch19-saint-omer-source-first.yml
      - src/core/chapterNineteenSaintOmerExpansion.ts
      - src/core/chapterNineteenSaintOmerExpansion.test.ts
      - src/ui/data/scenarioFilmStudyChapterNineteenSaintOmer.ts
      - src/ui/data/scenarioProductionVerificationSaintOmer.ts

permissions:
  contents: read

jobs:
  source-first:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Build core
        run: npm run build

      - name: Strict Saint Omer contract
        run: node --test dist/core/chapterNineteenSaintOmerExpansion.test.js

      - name: Build UI
        run: npm run build:ui
