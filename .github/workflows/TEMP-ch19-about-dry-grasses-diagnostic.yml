name: TEMP About Dry Grasses staging

on:
  workflow_dispatch:

jobs:
  dormant:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Staging source-first About Dry Grasses package; push trigger intentionally disabled."
