name: Materialize Chapter 17 representation audit

on:
  push:
    branches:
      - agent/chapter-17-representation-audit

permissions:
  contents: write

jobs:
  materialize:
    if: ${{ !contains(github.event.head_commit.message, '[chapter17-materialized]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/chapter-17-representation-audit
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Materialize representation audit
        run: node scripts/materializeChapter17RepresentationAudit.mjs
      - name: Commit permanent audit only
        run: |
          rm scripts/materializeChapter17RepresentationAudit.mjs
          rm .github/workflows/materialize-chapter-17-representation-audit.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/film/film_history_representation_coverage_v1.json scripts/materializeChapter17RepresentationAudit.mjs .github/workflows/materialize-chapter-17-representation-audit.yml
          git commit -m "Audit Chapter 17 representation coverage [chapter17-materialized]"
          git push origin HEAD:agent/chapter-17-representation-audit
