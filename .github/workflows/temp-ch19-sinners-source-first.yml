name: TEMP Chapter 19 Sinners source-first

on:
  push:
    branches:
      - agent/ch19-sinners-production-case
    paths:
      - .github/workflows/temp-ch19-sinners-source-first.yml
      - src/core/chapterNineteenSinnersExpansion.ts
      - src/core/chapterNineteenSinnersExpansion.test.ts
      - src/ui/data/scenarioProductionVerificationSinners.ts
      - src/ui/data/scenarioFilmStudyChapterNineteenSinners.ts

jobs:
  source-first:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout exact branch head
        uses: actions/checkout@v4
        with:
          ref: agent/ch19-sinners-production-case
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Build core
        run: npm run build

      - name: Strict Sinners contract
        run: node --test dist/core/chapterNineteenSinnersExpansion.test.js

      - name: Build UI
        run: npm run build:ui